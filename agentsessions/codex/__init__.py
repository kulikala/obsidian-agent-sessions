"""Integration with Codex CLI's own config (`~/.codex/config.toml`, respecting
`CODEX_HOME`) -- not to be confused with `agents.codex`, the scanning adapter
for Codex sessions as one of possibly several supported agents (same
distinction as `agentsessions.claude` vs. `agentsessions.agents.claude`; see
architecture.md §5.2)."""
